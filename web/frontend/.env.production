# Production build flags — picked up by Vite during `npm run build`.
# VITE_BASE controls the subpath under which the SPA is served; the
# GitHub Pages workflow overrides this at build time but a sensible
# default keeps local `npm run build` reproducible.
VITE_BASE=/Reactive-Robot/editor/
VITE_DEMO_MODE=true
